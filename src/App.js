import './App.css';
import Services from './components/Services/Services';

function App() {
  return (
    <div>
      <Services></Services>
      <h2>How React Work</h2>
      <p>mostly react work by app function with jsx. and reactDom.render method. and component is most important part of react. its work like body part of a react website or building blocks. and most easy part of a component we can build so many nested  component under app component by jsx. JSX is a syntex like HTML. but there have difference between JSX and HTML.</p>
      <h2>How UseState Work</h2>
      <p>UseState is a most important and useful function. and its a hook for use value to set in a function or show value on ui. we can use UseState in so many time like a object, a array, a boolean, in a class. its mostly useful on local component.  </p>
    </div>
  );
}

export default App;
